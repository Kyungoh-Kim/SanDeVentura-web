# Edge Function Tests

Planned Deno tests:

- `upload-session` rejects missing upload consent.
- Duplicate `idempotencyKey` returns duplicate success.
- Point validation returns accepted/rejected counts.
- `snap-position` returns on_route, caution, or away_from_route.

