# SanDeVentura web local environment template.
#
# Copy to .env.local and fill values from local Supabase or the target
# deployment. Do not commit real connection values or keys.

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=replace-with-local-or-deployed-anon-key
