# SanDeVentura web secret template.
#
# Copy to .secrets/local.secrets or another ignored file for local-only
# credentials. Keep service role keys, JWT secrets, passwords, and private keys
# out of git.

SUPABASE_SERVICE_ROLE_KEY=replace-with-local-service-role-key
SUPABASE_JWT_SECRET=replace-with-local-jwt-secret
